import { CreditCalculationParams, CreditCalculationResult } from "./credit-calculator-types";

export function getCreditCalculationResult(params: CreditCalculationParams): CreditCalculationResult {
    return { error: `Функция расчёта в разработке.` };
}